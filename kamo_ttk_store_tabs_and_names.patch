diff -ruN ttk_orig/Kamo-s-TTK-Store-main/_data/language.yml ttk_work/Kamo-s-TTK-Store-main/_data/language.yml
--- ttk_orig/Kamo-s-TTK-Store-main/_data/language.yml	2026-07-09 06:40:31.000000000 +0000
+++ ttk_work/Kamo-s-TTK-Store-main/_data/language.yml	2026-07-09 15:15:32.214864098 +0000
@@ -1,50 +1,51 @@
-# Tabs
-items_tab: 'Items'
-events_tab: 'Events'
-traits_tab: 'Traits'
-animals_tab: 'Animals'
-weapons_tab: 'Weapons'
-pawn_kinds_tab: 'Pawn Kinds'
-
-# Table headers
-mod_name_header: Mod Name
-author_header: Author
-version_header: Version
-command_name_header: Command Name
-user_level_header: User Level
-description_header: Description
-cost_header: Cost
-category_header: Category
-karma_type_header: Karma Type
-add_cost_header: Add Cost
-remove_cost_header: Remove Cost
-name_header: Name
-
-# Error messages
-no_commands: 'These are not the commands you are looking for.'
-
-# Redirect messages
-see_traits: 'See prices in the traits section'
-see_kinds: 'See prices in the pawn kinds section'
-see_items: 'See prices in the items section'
-
-# Search texts
-search_items: 'Search for items...'
-search_traits: 'Search for traits...'
-search_pawn_kinds: 'Search for pawn kinds...'
-search_mods: 'Search for mods...'
-search_commands: 'Search for commands...'
-search_animals: 'Search for animals...'
-search_events: 'Search for events...'
-search_weapons: 'Search for weapons...'
-
-# Metadata
-usage: 'Usage: %s'
-shortcut: 'This is a shortcut for %s'
-stats: 'Stats'
-conflicts_with: 'Conflicts with'
-from: 'From %s'
-weight: 'Weight'
-limit: 'Limit'
-karma: 'Karma'
+# Tabs
+items_tab: 'Items'
+events_tab: 'Events'
+traits_tab: 'Traits'
+animals_tab: 'Animals'
+weapons_tab: 'Weapons'
+apparel_tab: 'Apparel'
+pawn_kinds_tab: 'Pawn Kinds'
+
+# Table headers
+mod_name_header: Mod Name
+author_header: Author
+version_header: Version
+command_name_header: Command Name
+user_level_header: User Level
+description_header: Description
+cost_header: Cost
+category_header: Category
+karma_type_header: Karma Type
+add_cost_header: Add Cost
+remove_cost_header: Remove Cost
+name_header: Name
+
+# Error messages
+no_commands: 'These are not the commands you are looking for.'
+
+# Redirect messages
+see_traits: 'See prices in the traits section'
+see_kinds: 'See prices in the pawn kinds section'
+see_items: 'See prices in the items section'
+
+# Search texts
+search_items: 'Search for items...'
+search_traits: 'Search for traits...'
+search_pawn_kinds: 'Search for pawn kinds...'
+search_mods: 'Search for mods...'
+search_commands: 'Search for commands...'
+search_animals: 'Search for animals...'
+search_events: 'Search for events...'
+search_weapons: 'Search for weapons...'
+
+# Metadata
+usage: 'Usage: %s'
+shortcut: 'This is a shortcut for %s'
+stats: 'Stats'
+conflicts_with: 'Conflicts with'
+from: 'From %s'
+weight: 'Weight'
+limit: 'Limit'
+karma: 'Karma'
 bypasses_limit: 'This trait may exceed the trait limit'
\ No newline at end of file
diff -ruN ttk_orig/Kamo-s-TTK-Store-main/_includes/animal.html ttk_work/Kamo-s-TTK-Store-main/_includes/animal.html
--- ttk_orig/Kamo-s-TTK-Store-main/_includes/animal.html	2026-07-09 06:40:31.000000000 +0000
+++ ttk_work/Kamo-s-TTK-Store-main/_includes/animal.html	2026-07-09 15:15:32.214662474 +0000
@@ -49,7 +49,7 @@
     </td>
 
     <td>
-        {{- item.price -}}
+        {{- include.item.price -}}
 
         {%- if site.store.animals.weight and include.itemdata.Weight != 1 -%}
             <br/>
diff -ruN ttk_orig/Kamo-s-TTK-Store-main/_includes/tab.html ttk_work/Kamo-s-TTK-Store-main/_includes/tab.html
--- ttk_orig/Kamo-s-TTK-Store-main/_includes/tab.html	2026-07-09 06:40:31.000000000 +0000
+++ ttk_work/Kamo-s-TTK-Store-main/_includes/tab.html	2026-07-09 15:15:38.551134625 +0000
@@ -1,18 +1,24 @@
 <button class="tabLinks"
         onclick="changeTab(event, '{{- include.id -}}')">
 
-        {%- case include.id -%}
-            {%- when 'items' -%}
-                {{- site.data.language.items_tab -}}
-            {%- when 'events' -%}
-                {{- site.data.language.events_tab -}}
-            {%- when 'weapons' -%}
-                {{- site.data.language.weapons_tab -}}
-            {%- when 'animals' -%}
-                {{- site.data.language.animals_tab -}}
-            {%- when 'traits' -%}
-                {{- site.data.language.traits_tab -}}
-            {%- when 'kinds' -%}
-                {{- site.data.language.pawn_kinds_tab -}}
-        {%- endcase -%}
-</button>
\ No newline at end of file
+        {%- if include.label -%}
+            {{- include.label -}}
+        {%- else -%}
+            {%- case include.id -%}
+                {%- when 'items' -%}
+                    {{- site.data.language.items_tab -}}
+                {%- when 'events' -%}
+                    {{- site.data.language.events_tab -}}
+                {%- when 'weapons' -%}
+                    {{- site.data.language.weapons_tab -}}
+                {%- when 'apparel' -%}
+                    {{- site.data.language.apparel_tab | default: 'Apparel' -}}
+                {%- when 'animals' -%}
+                    {{- site.data.language.animals_tab -}}
+                {%- when 'traits' -%}
+                    {{- site.data.language.traits_tab -}}
+                {%- when 'kinds' -%}
+                    {{- site.data.language.pawn_kinds_tab -}}
+            {%- endcase -%}
+        {%- endif -%}
+</button>
diff -ruN ttk_orig/Kamo-s-TTK-Store-main/assets/js/display-names.js ttk_work/Kamo-s-TTK-Store-main/assets/js/display-names.js
--- ttk_orig/Kamo-s-TTK-Store-main/assets/js/display-names.js	2026-07-09 06:40:31.000000000 +0000
+++ ttk_work/Kamo-s-TTK-Store-main/assets/js/display-names.js	2026-07-09 15:18:03.414443090 +0000
@@ -12,13 +12,7 @@
     let name = shouldUseCommandName ? rawCommandName : rawDisplayName;
 
     name = applyManualCommandSpacing(name);
-
-    name = name
-        .replace(/[_-]+/g, " ")
-        .replace(/([a-z0-9])([A-Z])/g, "$1 $2")
-        .replace(/([A-Z]+)([A-Z][a-z])/g, "$1 $2")
-        .replace(/\s+/g, " ")
-        .trim();
+    name = normalizeNameSpacing(name);
 
     if (shouldUseCommandName) {
         return name;
@@ -38,10 +32,7 @@
         rawDisplayName.startsWith("HAR") ||
         rawDisplayName.startsWith("Aya");
 
-    const isHarMeat =
-        /^HAR\s+(CO\s+Race|EL\s+Monster).*Meat$/i.test(rawDisplayName);
-
-    return rawCommandName && (startsWithSpecialPrefix || isHarMeat);
+    return rawCommandName && startsWithSpecialPrefix;
 }
 
 function isHarMeatDisplayName(displayName) {
@@ -118,125 +109,90 @@
 
     return name;
 }
-function titleCaseKnownWords(text) {
-    let spaced = splitKnownSuffix(text);
 
-    spaced = spaced
+function normalizeNameSpacing(name) {
+    return name
         .replace(/[_-]+/g, " ")
         .replace(/([a-z0-9])([A-Z])/g, "$1 $2")
         .replace(/([A-Z]+)([A-Z][a-z])/g, "$1 $2")
         .replace(/\s+/g, " ")
         .trim();
-
-    return spaced
-        .split(/\s+/)
-        .map(capitalizeFirstLetter)
-        .join(" ");
 }
 
-function splitKnownSuffix(text) {
-    const knownSuffixes = [
-        "blouse",
-        "dress",
-        "helmet",
-        "armor",
-        "shirt",
-        "pants",
-        "robe",
-        "cape",
-        "hood",
-        "mask",
-        "veil",
-        "coat",
-        "jacket",
-        "boots",
-        "gloves",
-        "hat",
-        "crown",
-        "collar",
-        "belt"
+function removeDisplayOnlyTags(name) {
+    const tagPhrases = [
+        ["Vanilla", "Expanded"],
+        ["VAE", "Roy"],
+        ["VAE", "Waste"],
+        ["DP", "AT"],
+        ["LWM", "Deep", "Storage"],
+        ["Melee", "Weapon"],
+        ["Gun"],
+        ["Raw"],
+        ["Fish"],
+        ["VAE"],
+        ["AEXP"],
+        ["VPE"],
+        ["VWE"],
+        ["VBE"],
+        ["VCE"],
+        ["VREA"],
+        ["MP"],
+        ["AT"],
+        ["SAB"],
+        ["LWM"],
+        ["DR"],
+        ["DBH"],
+        ["BMOT"],
+        ["GM"],
+        ["SBC"],
+        ["DP"],
+        ["Apparel"],
+        ["Footwear"],
+        ["Headgear"],
+        ["Handwear"],
+        ["Building"],
+        ["Relic"],
+        ["Inert"],
+        ["Mawy"],
+        ["Industrial"]
     ];
 
-    const lowerText = text.toLowerCase();
-
-    for (const suffix of knownSuffixes) {
-        if (lowerText.endsWith(suffix) && lowerText.length > suffix.length) {
-            const firstPart = text.slice(0, text.length - suffix.length);
-            const lastPart = text.slice(text.length - suffix.length);
-            return firstPart + " " + lastPart;
-        }
-    }
-
-    return text;
+    return removeTagPhrases(name, tagPhrases);
 }
 
-function capitalizeFirstLetter(word) {
-    if (!word) {
-        return word;
-    }
-
-    return word.charAt(0).toUpperCase() + word.slice(1).toLowerCase();
-}
-
-function capitalizeFirstAndLastWords(name) {
-    const words = name.split(/\s+/);
-
-    if (words.length === 0) {
-        return name;
-    }
-
-    words[0] = capitalizeFirstLetter(words[0]);
+function removeTagPhrases(name, tagPhrases) {
+    let words = name.split(/\s+/).filter(Boolean);
+    let changed = true;
+
+    while (changed) {
+        changed = false;
+
+        for (const phrase of tagPhrases) {
+            for (let i = 0; i <= words.length - phrase.length; i++) {
+                const matches = phrase.every(function (tagWord, offset) {
+                    return words[i + offset] === tagWord;
+                });
+
+                if (matches) {
+                    words.splice(i, phrase.length);
+                    changed = true;
+                    break;
+                }
+            }
 
-    if (words.length > 1) {
-        const lastIndex = words.length - 1;
-        words[lastIndex] = capitalizeFirstLetter(words[lastIndex]);
+            if (changed) {
+                break;
+            }
         }
-
-    return words.join(" ");
-    }
-
-function capitalizeFirstLetter(word) {
-    if (!word) {
-        return word;
     }
 
-    return word.charAt(0).toUpperCase() + word.slice(1);
+    return words.join(" ").trim();
 }
 
-function removeDisplayOnlyTags(name) {
-        const removableTags = new Set([
-            "Apparel",
-            "Armor",
-            "Footwear",
-            "Headgear",
-            "Handwear",
-            "Building",
-            "Relic",
-            "Inert",
-            "Simple",
-            "Mawy",
-            "Industrial"
-        ]);
-    
-        return name
-            .split(/\s+/)
-            .filter(word => !removableTags.has(word))
-            .join(" ")
-            .trim();
-    }
 function movePriorityTagsToFront(name) {
     const priorityTags = [
-        "Prestige",
-        "VPE",
-        "VAE",
-        "DBH",
-        "VCE",
-        "VREA",
-        "AEXP",
-        "LWM",
-        "VBE",
-        "AT"
-        
+        "Prestige"
     ];
 
     let words = name.split(/\s+/);
@@ -248,20 +204,22 @@
         if (index !== -1) {
             foundTags.push(tag);
             words.splice(index, 1);
-            }
-        });
+        }
+    });
 
     if (foundTags.length === 0) {
         return name;
-        }
+    }
 
     return [...foundTags, ...words].join(" ");
-    }
+}
 
 function moveLeadingTagsToEnd(name) {
     const tagPhrases = [
         ["Animal", "Vaccine"],
-        ["Vaccine"]
+        ["Vaccine"],
+        ["Targeter"],
+        ["Psytrainer"]
     ];
 
     const words = name.split(/\s+/);
@@ -272,11 +230,11 @@
         if (startsWithTag && words.length > tagWords.length) {
             const remainingWords = words.slice(tagWords.length);
             return [...remainingWords, ...tagWords].join(" ");
-            }
         }
+    }
 
     return name;
-    }
+}
 
 function reorderEggName(name) {
     const words = name.split(/\s+/);
@@ -306,6 +264,19 @@
         return name;
     }
 
+    const leadingClarifierPhrases = [
+        ["Mech", "Serum"],
+        ["Nature", "Shrine"]
+    ];
+
+    for (const phrase of leadingClarifierPhrases) {
+        const startsWithPhrase = phrase.every((word, index) => words[index] === word);
+
+        if (startsWithPhrase && words.length > phrase.length) {
+            return [...words.slice(phrase.length), ...phrase].join(" ");
+        }
+    }
+
     const leadingClarifiers = new Set([
         "Meat",
         "Leather",
@@ -316,17 +287,14 @@
         "Shell",
         "Subcore",
         "Medicine",
-        "MechSerum",
         "Pack",
         "Sculpture",
-        "Nature Shrine",
         "Radiator",
         "Blocks",
         "Turret",
         "Pallet",
         "Crown",
         "Techprint"
-        
     ]);
 
     const firstWord = words[0];
@@ -338,6 +306,60 @@
     return name;
 }
 
+function titleCaseKnownWords(text) {
+    let spaced = splitKnownSuffix(text);
+    spaced = normalizeNameSpacing(spaced);
+
+    return spaced
+        .split(/\s+/)
+        .map(capitalizeFirstLetter)
+        .join(" ");
+}
+
+function splitKnownSuffix(text) {
+    const knownSuffixes = [
+        "blouse",
+        "dress",
+        "helmet",
+        "armor",
+        "shirt",
+        "pants",
+        "robe",
+        "cape",
+        "hood",
+        "mask",
+        "veil",
+        "coat",
+        "jacket",
+        "boots",
+        "gloves",
+        "hat",
+        "crown",
+        "collar",
+        "belt"
+    ];
+
+    const lowerText = text.toLowerCase();
+
+    for (const suffix of knownSuffixes) {
+        if (lowerText.endsWith(suffix) && lowerText.length > suffix.length) {
+            const firstPart = text.slice(0, text.length - suffix.length);
+            const lastPart = text.slice(text.length - suffix.length);
+            return firstPart + " " + lastPart;
+        }
+    }
+
+    return text;
+}
+
+function capitalizeFirstLetter(word) {
+    if (!word) {
+        return word;
+    }
+
+    return word.charAt(0).toUpperCase() + word.slice(1).toLowerCase();
+}
+
 window.addEventListener("DOMContentLoaded", function () {
     document.querySelectorAll(".store-display-name").forEach(function (element) {
         const displayName = element.textContent.trim();
diff -ruN ttk_orig/Kamo-s-TTK-Store-main/index.html ttk_work/Kamo-s-TTK-Store-main/index.html
--- ttk_orig/Kamo-s-TTK-Store-main/index.html	2026-07-09 06:40:31.000000000 +0000
+++ ttk_work/Kamo-s-TTK-Store-main/index.html	2026-07-09 15:16:32.414732936 +0000
@@ -7,32 +7,115 @@
 
 {%- assign itemsForDisplay = '' | split: ',' -%}
 {%- assign eventsForDisplay = site.data.StoreIncidents.incitems | where_exp: "incident", "incident.price > 0" | sort: "abr" -%}
-{%- assign animalsForDisplay = site.data.StoreItems.items | where: "category", "Animal" | where_exp: "animal", "animal.price > 0" | sort: "abr" -%}
+{%- assign animalsForDisplay = '' | split: ',' -%}
+{%- assign apparelForDisplay = '' | split: ',' -%}
 {%- assign traitsForDisplay = '' | split: ',' -%}
 {%- assign kindsForDisplay = site.data.ShopExt.races | where: "enabled", "true" | sort: "name" -%}
 {%- assign weaponsForDisplay = '' | split: ',' -%}
 
+{%- assign vePsycastsForDisplay = '' | split: ',' -%}
+{%- assign veAnimalsForDisplay = '' | split: ',' -%}
+{%- assign veWeaponsForDisplay = '' | split: ',' -%}
+{%- assign veCookingForDisplay = '' | split: ',' -%}
+{%- assign veBrewingForDisplay = '' | split: ',' -%}
+{%- assign veApparelForDisplay = '' | split: ',' -%}
+{%- assign veArmourForDisplay = '' | split: ',' -%}
+{%- assign veAndroidForDisplay = '' | split: ',' -%}
+{%- assign veAnimalsEndangeredForDisplay = '' | split: ',' -%}
+{%- assign vePlantsForDisplay = '' | split: ',' -%}
+{%- assign veAnimalsWasteForDisplay = '' | split: ',' -%}
+{%- assign veAnimalsRoyalForDisplay = '' | split: ',' -%}
+{%- assign veBooksForDisplay = '' | split: ',' -%}
+{%- assign veHairForDisplay = '' | split: ',' -%}
+
 {%- for item in site.data.StoreItems.items -%}
 {%- if item.price <= 0 -%}
 {%- continue -%}
 {%- endif -%}
 
-{%- if site.data.itemdata[item.defname].IsWeapon -%}
-{%- assign weaponsForDisplay = weaponsForDisplay | push: item | sort: "abr" -%}
+{%- assign itemdata = site.data.itemdata[item.defname] -%}
+{%- assign modName = itemdata.Mod -%}
+{%- assign categoryName = item.category | default: '' -%}
+{%- assign categoryDown = categoryName | downcase -%}
+{%- assign defNameDown = item.defname | default: '' | downcase -%}
+
+{%- if modName == 'Vanilla Psycasts Expanded' -%}
+{%- assign vePsycastsForDisplay = vePsycastsForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Animals Expanded' -%}
+{%- assign veAnimalsForDisplay = veAnimalsForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Weapons Expanded' -%}
+{%- assign veWeaponsForDisplay = veWeaponsForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Cooking Expanded' -%}
+{%- assign veCookingForDisplay = veCookingForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Brewing Expanded' -%}
+{%- assign veBrewingForDisplay = veBrewingForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Apparel Expanded' -%}
+{%- assign veApparelForDisplay = veApparelForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Armour Expanded' -%}
+{%- assign veArmourForDisplay = veArmourForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Races Expanded - Android' -%}
+{%- assign veAndroidForDisplay = veAndroidForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Animals Expanded — Endangered' -%}
+{%- assign veAnimalsEndangeredForDisplay = veAnimalsEndangeredForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Plants Expanded' -%}
+{%- assign vePlantsForDisplay = vePlantsForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Animals Expanded — Waste Animals' -%}
+{%- assign veAnimalsWasteForDisplay = veAnimalsWasteForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Animals Expanded — Royal Animals' -%}
+{%- assign veAnimalsRoyalForDisplay = veAnimalsRoyalForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Books Expanded' -%}
+{%- assign veBooksForDisplay = veBooksForDisplay | push: item -%}
+{%- elsif modName == 'Vanilla Hair Expanded' -%}
+{%- assign veHairForDisplay = veHairForDisplay | push: item -%}
+{%- endif -%}
+
+{%- assign isEggItem = false -%}
+{%- if categoryName contains 'Egg' -%}
+{%- assign isEggItem = true -%}
+{%- endif -%}
+
+{%- assign isApparelItem = false -%}
+{%- if defNameDown contains 'apparel' or categoryDown contains 'apparel' or categoryDown contains 'clothing' or categoryDown contains 'headgear' or categoryDown contains 'armor' or categoryDown contains 'armour' or categoryDown contains 'handwear' or categoryDown contains 'footwear' or categoryDown contains 'shield' or categoryDown contains 'onskin' or categoryDown contains 'overhead' or categoryDown contains 'middle' or categoryDown contains 'utility' -%}
+{%- assign isApparelItem = true -%}
+{%- endif -%}
+
+{%- if categoryName == 'Animals' or categoryName == 'Animal' or isEggItem -%}
+{%- assign animalsForDisplay = animalsForDisplay | push: item -%}
 {%- continue -%}
 {%- endif -%}
 
-{%- if item.category == "Animals" -%}
-{%- assign animalsForDisplay = animalsForDisplay | push: item | sort: "abr" -%}
+{%- if isApparelItem -%}
+{%- assign apparelForDisplay = apparelForDisplay | push: item -%}
 {%- continue -%}
 {%- endif -%}
 
-{%- if item.category != "Animal" -%}
-{%- assign itemsForDisplay = itemsForDisplay | push: item | sort: "abr" -%}
+{%- if itemdata.IsWeapon -%}
+{%- assign weaponsForDisplay = weaponsForDisplay | push: item -%}
 {%- continue -%}
 {%- endif -%}
+
+{%- assign itemsForDisplay = itemsForDisplay | push: item -%}
 {%- endfor -%}
 
+{%- assign itemsForDisplay = itemsForDisplay | sort: 'abr' -%}
+{%- assign animalsForDisplay = animalsForDisplay | sort: 'abr' -%}
+{%- assign apparelForDisplay = apparelForDisplay | sort: 'abr' -%}
+{%- assign weaponsForDisplay = weaponsForDisplay | sort: 'abr' -%}
+{%- assign vePsycastsForDisplay = vePsycastsForDisplay | sort: 'abr' -%}
+{%- assign veAnimalsForDisplay = veAnimalsForDisplay | sort: 'abr' -%}
+{%- assign veWeaponsForDisplay = veWeaponsForDisplay | sort: 'abr' -%}
+{%- assign veCookingForDisplay = veCookingForDisplay | sort: 'abr' -%}
+{%- assign veBrewingForDisplay = veBrewingForDisplay | sort: 'abr' -%}
+{%- assign veApparelForDisplay = veApparelForDisplay | sort: 'abr' -%}
+{%- assign veArmourForDisplay = veArmourForDisplay | sort: 'abr' -%}
+{%- assign veAndroidForDisplay = veAndroidForDisplay | sort: 'abr' -%}
+{%- assign veAnimalsEndangeredForDisplay = veAnimalsEndangeredForDisplay | sort: 'abr' -%}
+{%- assign vePlantsForDisplay = vePlantsForDisplay | sort: 'abr' -%}
+{%- assign veAnimalsWasteForDisplay = veAnimalsWasteForDisplay | sort: 'abr' -%}
+{%- assign veAnimalsRoyalForDisplay = veAnimalsRoyalForDisplay | sort: 'abr' -%}
+{%- assign veBooksForDisplay = veBooksForDisplay | sort: 'abr' -%}
+{%- assign veHairForDisplay = veHairForDisplay | sort: 'abr' -%}
+
 {%- for trait in site.data.ShopExt.traits -%}
 {%- if trait.canAdd == true or trait.canRemove == true -%}
 {%- assign traitsForDisplay = traitsForDisplay | push: trait | sort: "name" -%}
@@ -49,6 +132,14 @@
 {%- endif -%}
 {%- endfor -%}
 
+{%- assign itemHeaders = '' | split: ',' -%}
+{%- assign itemHeaders = itemHeaders | push: site.data.language.name_header -%}
+{%- assign itemHeaders = itemHeaders | push: site.data.language.cost_header -%}
+{%- if site.store.items.category -%}
+{%- assign itemHeaders = itemHeaders | push: site.data.language.category_header -%}
+{%- endif -%}
+{%- assign itemHeaders = itemHeaders | join: ',' -%}
+
 <div class="tab">
     {%- if itemsForDisplay.size > 0 -%}
     {%- include tab.html id='items' -%}
@@ -62,6 +153,10 @@
     {%- include tab.html id='weapons' -%}
     {%- endif -%}
 
+    {%- if apparelForDisplay.size > 0 -%}
+    {%- include tab.html id='apparel' -%}
+    {%- endif -%}
+
     {%- if animalsForDisplay.size > 0 -%}
     {%- include tab.html id='animals' -%}
     {%- endif -%}
@@ -73,24 +168,29 @@
     {%- if kindsForDisplay.size > 0 -%}
     {%- include tab.html id='kinds' -%}
     {%- endif -%}
+
+    {%- if vePsycastsForDisplay.size > 0 -%}{%- include tab.html id='ve_psycasts' label='VE Psycasts' -%}{%- endif -%}
+    {%- if veAnimalsForDisplay.size > 0 -%}{%- include tab.html id='ve_animals' label='VE Animals' -%}{%- endif -%}
+    {%- if veAnimalsRoyalForDisplay.size > 0 -%}{%- include tab.html id='ve_animals_royal' label='VE Royal Animals' -%}{%- endif -%}
+    {%- if veAnimalsEndangeredForDisplay.size > 0 -%}{%- include tab.html id='ve_animals_endangered' label='VE Endangered Animals' -%}{%- endif -%}
+    {%- if veAnimalsWasteForDisplay.size > 0 -%}{%- include tab.html id='ve_animals_waste' label='VE Waste Animals' -%}{%- endif -%}
+    {%- if veWeaponsForDisplay.size > 0 -%}{%- include tab.html id='ve_weapons' label='VE Weapons' -%}{%- endif -%}
+    {%- if veCookingForDisplay.size > 0 -%}{%- include tab.html id='ve_cooking' label='VE Cooking' -%}{%- endif -%}
+    {%- if veBrewingForDisplay.size > 0 -%}{%- include tab.html id='ve_brewing' label='VE Brewing' -%}{%- endif -%}
+    {%- if veApparelForDisplay.size > 0 -%}{%- include tab.html id='ve_apparel' label='VE Apparel' -%}{%- endif -%}
+    {%- if veArmourForDisplay.size > 0 -%}{%- include tab.html id='ve_armour' label='VE Armour' -%}{%- endif -%}
+    {%- if veAndroidForDisplay.size > 0 -%}{%- include tab.html id='ve_android' label='VE Android' -%}{%- endif -%}
+    {%- if vePlantsForDisplay.size > 0 -%}{%- include tab.html id='ve_plants' label='VE Plants' -%}{%- endif -%}
+    {%- if veBooksForDisplay.size > 0 -%}{%- include tab.html id='ve_books' label='VE Books' -%}{%- endif -%}
+    {%- if veHairForDisplay.size > 0 -%}{%- include tab.html id='ve_hair' label='VE Hair' -%}{%- endif -%}
 </div>
 
 {%- if itemsForDisplay.size > 0 -%}
-{%- assign headers = '' | split: ',' -%}
-{%- assign headers = headers | push: site.data.language.name_header -%}
-{%- assign headers = headers | push: site.data.language.cost_header -%}
-
-{%- if site.store.items.category -%}
-{%- assign headers = headers | push: site.data.language.category_header -%}
-{%- endif -%}
-
-{%- assign headers = headers | join: ',' -%}
-
 {%- include store_tab.html
 id='items'
 type='Item'
 buyName=buy
-headers=headers
+headers=itemHeaders
 priceDataHeaders='Cost'
 displayData=itemsForDisplay
 placeholder=site.data.language.search_items -%}
@@ -126,6 +226,17 @@
 placeholder=site.data.language.search_weapons -%}
 {%- endif -%}
 
+{%- if apparelForDisplay.size > 0 -%}
+{%- include store_tab.html
+id='apparel'
+type='Item'
+buyName=buy
+headers=itemHeaders
+priceDataHeaders='Cost'
+displayData=apparelForDisplay
+placeholder='Search for apparel...' -%}
+{%- endif -%}
+
 {%- if animalsForDisplay.size > 0 -%}
 {% capture headers %}
 {{ site.data.language.name_header }},{{ site.data.language.cost_header }}
@@ -181,6 +292,22 @@
 placeholder=site.data.language.search_pawn_kinds -%}
 {%- endif -%}
 
+{%- assign veTabs = '' | split: ',' -%}
+
+{%- if vePsycastsForDisplay.size > 0 -%}{%- include store_tab.html id='ve_psycasts' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=vePsycastsForDisplay placeholder='Search VE Psycasts...' -%}{%- endif -%}
+{%- if veAnimalsForDisplay.size > 0 -%}{%- include store_tab.html id='ve_animals' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veAnimalsForDisplay placeholder='Search VE Animals...' -%}{%- endif -%}
+{%- if veAnimalsRoyalForDisplay.size > 0 -%}{%- include store_tab.html id='ve_animals_royal' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veAnimalsRoyalForDisplay placeholder='Search VE Royal Animals...' -%}{%- endif -%}
+{%- if veAnimalsEndangeredForDisplay.size > 0 -%}{%- include store_tab.html id='ve_animals_endangered' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veAnimalsEndangeredForDisplay placeholder='Search VE Endangered Animals...' -%}{%- endif -%}
+{%- if veAnimalsWasteForDisplay.size > 0 -%}{%- include store_tab.html id='ve_animals_waste' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veAnimalsWasteForDisplay placeholder='Search VE Waste Animals...' -%}{%- endif -%}
+{%- if veWeaponsForDisplay.size > 0 -%}{%- include store_tab.html id='ve_weapons' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veWeaponsForDisplay placeholder='Search VE Weapons...' -%}{%- endif -%}
+{%- if veCookingForDisplay.size > 0 -%}{%- include store_tab.html id='ve_cooking' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veCookingForDisplay placeholder='Search VE Cooking...' -%}{%- endif -%}
+{%- if veBrewingForDisplay.size > 0 -%}{%- include store_tab.html id='ve_brewing' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veBrewingForDisplay placeholder='Search VE Brewing...' -%}{%- endif -%}
+{%- if veApparelForDisplay.size > 0 -%}{%- include store_tab.html id='ve_apparel' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veApparelForDisplay placeholder='Search VE Apparel...' -%}{%- endif -%}
+{%- if veArmourForDisplay.size > 0 -%}{%- include store_tab.html id='ve_armour' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veArmourForDisplay placeholder='Search VE Armour...' -%}{%- endif -%}
+{%- if veAndroidForDisplay.size > 0 -%}{%- include store_tab.html id='ve_android' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veAndroidForDisplay placeholder='Search VE Android...' -%}{%- endif -%}
+{%- if vePlantsForDisplay.size > 0 -%}{%- include store_tab.html id='ve_plants' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=vePlantsForDisplay placeholder='Search VE Plants...' -%}{%- endif -%}
+{%- if veBooksForDisplay.size > 0 -%}{%- include store_tab.html id='ve_books' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veBooksForDisplay placeholder='Search VE Books...' -%}{%- endif -%}
+{%- if veHairForDisplay.size > 0 -%}{%- include store_tab.html id='ve_hair' type='Item' buyName=buy headers=itemHeaders priceDataHeaders='Cost' displayData=veHairForDisplay placeholder='Search VE Hair...' -%}{%- endif -%}
 
 <script>
     window.addEventListener("load", function (event) {
